<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GeneradordePasswords" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="font">@font/roboto_semibold</item>
        <item name="android:textColor">@color/white</item>
        <item name="backgroundColor">@color/background1</item>
        <item name="background">@drawable/background</item>

    </style>

    <style name="Theme.GeneradordePasswords" parent="Base.Theme.GeneradordePasswords"/>
</resources>